node_modules
dist
.vite
*.local
.env
.env.*
!.env.example
!.env.staging

# TypeScript incremental build & vite emit artifacts (not source)
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# Brainstorming visual companion mockups
.superpowers/

# Per-worktree dev-server config (agent tooling), not part of the app
.claude/launch.json
