# Copy to `.env.local` to override the dev-server proxy target.
# Where `vite dev` proxies /api and /oauth. Defaults to http://localhost:8085.
# For the shared staging backend, prefer `npm run dev:staging` (uses .env.staging).
VITE_API_TARGET=http://localhost:8085
